/* 
    R. Alex Clark
*/

(function($) {

    // Site Settings
    var settings = {};

    $(function() {

        // Setup variables
        var $window = $(window),
            $document = $(document),
            $body = $('body'),
            $html = $('html'),
            $bodyHtml = $('bodyHtml'),
            $wrapper = $('#wrapper');

        // Setup loading class for body
        $body.addClass('is-loading');

    });

})(jQuery);